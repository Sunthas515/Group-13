function int = trapezoidal_integration(f, n)
% Takes in a function and returns the integral of it given that
% f = f(x)
% n = number of subintervals
% f(0) = a
% f(end) = b
% h = (b-a)/n
sum = 0;
for i=2:size(f)-1
    sum = sum + f(i);
end
volume = 
end